import spacing from './spacing';
import palette from './palette';
import typography from './typography';

/* Add theme files here then import "theme" in your component */
export const theme = {
  spacing,
  palette,
  typography,
};
